package {{ paquete_base }}.controller;

import {{ paquete_base }}.dto.request.{{ nombre_clase }}Request;
import {{ paquete_base }}.dto.response.{{ nombre_clase }}Response;
import {{ paquete_base }}.service.{{ nombre_clase }}Service;
import jakarta.validation.Valid;
import org.springframework.http.HttpStatus;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.*;

import java.util.List;

@RestController
@RequestMapping("/api/{{ nombre_recurso }}")
public class {{ nombre_clase }}Controller {

    private final {{ nombre_clase }}Service service;

    public {{ nombre_clase }}Controller({{ nombre_clase }}Service service) {
        this.service = service;
    }

    @GetMapping
    public List<{{ nombre_clase }}Response> listar() {
        return service.listar();
    }

    {{ anotacion_obtener }}
    public ResponseEntity<{{ nombre_clase }}Response> obtener({{ parametros_id_controller }}) {
        return ResponseEntity.ok(service.obtener({{ argumentos_id }}));
    }

    @PostMapping
    @ResponseStatus(HttpStatus.CREATED)
    public {{ nombre_clase }}Response crear(@Valid @RequestBody {{ nombre_clase }}Request datos) {
        return service.crear(datos);
    }

    {{ anotacion_actualizar }}
    public {{ nombre_clase }}Response actualizar({{ parametros_id_controller }}, @Valid @RequestBody {{ nombre_clase }}Request datos) {
        return service.actualizar({{ argumentos_id }}, datos);
    }

    {{ anotacion_eliminar }}
    @ResponseStatus(HttpStatus.NO_CONTENT)
    public void eliminar({{ parametros_id_controller }}) {
        service.eliminar({{ argumentos_id }});
    }
}
